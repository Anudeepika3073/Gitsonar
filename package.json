{
  "name": "@capire/sflight",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "lint": "eslint . -f checkstyle -o lint.xml",
    "lint:sonar": "eslint . -f sonarqube -o eslint-report.json",
    "test": "jest"
  },
  "devDependencies": {
    "eslint": "9.18.0",
    "eslint-formatter-sonarqube": "^1.0.0",
    "jest": "^30.1.0"
  }
}
