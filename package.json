{
  "name": "@capire/sflight",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "lint": "eslint .",
    "lint:sonar": "eslint . -f sonarqube -o eslint-report.json",
    "test": "jest --coverage"
  },
  "devDependencies": {
    "eslint": "^9.0.0",
    "eslint-formatter-sonarqube": "^1.0.0",
    "jest": "^30.1.0"
  }
}
