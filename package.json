{
  "name": "@capire/sflight",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "lint": "eslint .",
    "lint:sonar": "eslint . --ext .js,.ts -f json -o eslint-report.json --no-error-on-unmatched-pattern --cwd .",
    "test": "jest --coverage"
  },
  "devDependencies": {
    "eslint": "^7.32.0",
    "eslint-formatter-sonarqube": "^1.0.0",
    "jest": "^30.1.0"
  }
}
